# Authorized testing evidence

The `local_header_audit` module reviews response-security headers and rejects
every target that is not `localhost` or a loopback IP. Tests verify both the
allowed and rejected target classes.

This demonstrates enforcement of authorization scope; it is not evidence of a
third-party penetration test.

