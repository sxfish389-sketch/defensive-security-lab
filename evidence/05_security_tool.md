# Security-tool development evidence

The repository packages four defensive checks behind a standard-library Python
CLI and tests them with `unittest`. GitHub Actions runs the same tests after
publication.

The project favors clear safety boundaries and reproducible output over attack
automation.

